# Netlify 部署环境变量配置示例
# 请在 Netlify 控制台的 Site settings > Environment variables 中配置这些变量

# 应用环境
NODE_ENV=production

# DeepSeek API 配置
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# JWT 密钥（用于用户认证）
JWT_SECRET=your_jwt_secret_key_here

# 数据库配置（如果使用 MySQL）
DB_HOST=your_database_host
DB_USER=your_database_user
DB_PASSWORD=your_database_password
DB_NAME=your_database_name

# SQLite 数据库路径（Netlify Functions 中使用临时存储）
SQLITE_DB_PATH=/tmp/database.sqlite

# CORS 允许的域名（可选，默认允许 Netlify 域名）
ALLOWED_ORIGINS=https://your-app.netlify.app,https://your-custom-domain.com

# 文件上传配置
MAX_FILE_SIZE=10485760
UPLOAD_DIR=/tmp/uploads

# 其他配置
PORT=3001